# 课程网站使用手册（老师版）

> 写给完全不懂 git 的老师/助教。你只需要会两件事：**打开文件夹、编辑保存**。
> 其他全部自动完成。

## 一、这个系统帮你做什么

你编辑课程文件，保存后，课程网站**自动更新**。

```
你编辑文件并保存
    ↓（全自动）
网站更新
```

你不需要知道 git、服务器、构建这些词是什么意思。

## 二、第一次设置（只做一次）

### 前提

你的电脑要连接**实验室网络**（能访问实验室服务器的网络）。

### 把课程文件夹挂到你的电脑

1. 打开"此电脑"（或任意文件夹窗口）
2. 点顶部地址栏，输入：

```
\\10.10.10.41\course-content
```

3. 回车后弹登录框：
   - 用户名：`course`
   - 密码：`course2026`

4. 看到一个文件夹内容，就成功了。

### 设置成"Z 盘"（可选但推荐）

这样以后直接点 Z 盘就进课程文件夹，不用每次输地址。

1. 右键点刚才的文件夹窗口左侧的"此电脑"
2. 选"映射网络驱动器"
3. 驱动器选 `Z:`
4. 文件夹填 `\\10.10.10.41\course-content`
5. 勾选"登录时重新连接"
6. 点完成

以后打开"此电脑"，双击 `Z:` 盘就是课程内容。

## 三、日常更新：三步

1. 打开 Z 盘
2. 找到要改的文件，双击编辑（用记事本或 VS Code）
3. 保存

**完事。** 等 1-3 分钟，网站自动更新。

> 不用点任何"上传""同步"按钮，保存就是全部操作。

## 四、怎么修改已有内容

### 改文字

课程页面都是 `.md` 文件（Markdown 格式）。用记事本打开，直接改文字，保存。

常用格式：

| 想写什么 | 怎么写 |
|---|---|
| 大标题 | `# 标题` |
| 小标题 | `## 小标题` |
| 加粗 | `**加粗**` |
| 列表 | 每行开头 `- ` |
| 代码 | 上下各三个反引号 \`\`\` 包住 |

### 替换课件文件

找到同名文件，用新的替换，保存即可。

## 五、添加新章节（较少用）

新章节 = 新文件夹 + 新 `.md` 文件。

1. 在 Z 盘的 `docs/` 文件夹里新建文件夹，名字带数字前缀，例如：

```
04-爬虫入门/
```

2. 在里面新建 `爬虫入门.md`，写内容
3. 保存

章节会自动出现在网站左侧导航。

> 如果你希望导航顺序/名字特别定制，告诉开发者帮你调整，不用自己研究。

## 六、添加课件

### PPT 课件

把 `.pptx` 文件放进 `docs/` 对应章节目录，保存。网站会自动把它变成可在线浏览的幻灯片。

### 图片

把图片（png/jpg）放进 `docs/assets/gallery/`，保存。图片会自动出现在画廊页。

### 视频

把视频文件（mp4 格式）直接放进 Z 盘的 `media/videos/` 文件夹，保存。

视频会自动出现在网站左侧导航的「课程视频」页面里，可以直接在线播放。

> 推荐 mp4 格式（浏览器兼容性最好）。视频文件较大时，上传可能需要几分钟，耐心等待。

## 七、查看网站

在实验室网络内，浏览器打开：

```
http://10.10.10.41:8080/
```

## 八、重要：别碰这些

Z 盘里有些文件是"系统文件"，**不要修改、不要删除**：

- `mkdocs.yml`（网站配置）
- `hooks/` 文件夹（构建程序）
- `Dockerfile`、`docker-compose.yml`、`.gitlab-ci.yml`
- 任何以 `.` 开头的隐藏文件

**你只需要动 `docs/` 文件夹里的东西。**

## 九、常见问题

### 改了内容，网站没变化？

- 等 1-3 分钟（自动更新需要时间）
- 确认保存了文件
- 强制刷新浏览器（Ctrl + F5）

### Z 盘打不开？

- 确认连的是实验室网络
- 确认输入了 `\\10.10.10.41\course-content`（双反斜杠）

### 误删了文件怎么办？

别慌，立刻联系开发者，内容有版本记录，可以恢复。

## 十、一句话总结

**打开 Z 盘 → 改文件 → 保存。** 就这三步。

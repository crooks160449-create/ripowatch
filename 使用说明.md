# RepoWatch 课程网站使用说明

> 适用对象：课程老师、助教、维护课程内容的同学
> 无需了解 Python，只需会文件操作和复制几条命令

## 一、先认识三个东西

整个网站由两个部分构成，都在项目的根目录里：

```
repo-watcher/
├── docs/           ← 所有课程内容都放在这里
│   ├── index.md    ← 网站首页
│   ├── 01-课程概述与环境搭建/   ← 一个章节就是一个文件夹
│   ├── 02-Python基础语法/
│   └── 资源下载/
└── mkdocs.yml      ← 网站的“导航配置”，决定左侧菜单长什么样
```

**记忆口诀：**

- 改内容 → 改 `docs/` 里的文件
- 改左侧菜单 → 改 `mkdocs.yml`
- 改完要让线上更新 → 执行 `git push`（下面每条操作都写好了命令）

## 二、开始前：确认能推送到仓库

以下操作只需要在项目根目录打开一个 PowerShell 窗口。打开方式：

1. 打开项目文件夹
2. 在文件夹空白处按住 `Shift`，点鼠标右键
3. 选择“在此处打开 PowerShell 窗口”

以后所有 `git` 命令都复制到这个窗口里回车执行。

## 三、操作一：修改一个已有的 Markdown 页面

### 适用场景

改错别字、更新讲义内容、替换代码示例。

### 步骤

1. 在 `docs/` 里找到对应的 `.md` 文件，用任意文本编辑器打开
   - Windows 记事本可以
   - 推荐 VS Code（有颜色提示，看着更清楚）
2. 修改内容，保存
3. 在 PowerShell 窗口依次执行：

```powershell
git add .

git commit -m "更新课程介绍内容"

git push
```

4. 等待 1～2 分钟，刷新网站即可看到新内容

### 修改 `mkdocs.yml` 时同理

只要动了 `docs/` 里的文件或者 `mkdocs.yml`，最后都执行上面三条 `git` 命令。

## 四、操作二：添加一个新章节

### 示例目标

新加一个章节“04-爬虫入门”，里面有一页“爬虫基础”。

### 步骤

1. 在 `docs/` 文件夹里新建一个文件夹，命名建议带数字前缀以便排序：

```
docs/04-爬虫入门/
```

2. 在这个文件夹里新建一个 Markdown 文件：

```
docs/04-爬虫入门/爬虫基础.md
```

3. 用文本编辑器打开这个 `.md` 文件，写下内容。最简单的格式：

```markdown
# 爬虫基础

## 什么是爬虫

爬虫是自动抓取网页内容的程序。

## 示例代码

```python
import requests

print("Hello")
```
```

> 注意：上面例子里的代码块前后三个反引号 `` ``` `` 需要成对出现。

4. 打开项目根目录的 `mkdocs.yml`，找到 `nav:` 一段，在合适位置插入两行：

```yaml
nav:
  - 首页: index.md
  - 课程概述与环境搭建:
    ...
  - Python 基础语法:
    ...
  - 爬虫入门:                                    # 新增的章节标题
    - 爬虫基础: 04-爬虫入门/爬虫基础.md           # 新增的页面
  - 资源下载:
    ...
```

**关键规则：缩进必须对齐。** 章节标题前面有两个空格，页面标题前面有四个空格，冒号后面一个空格。

5. 保存 `mkdocs.yml`，在 PowerShell 执行：

```powershell
git add .

git commit -m "新增爬虫入门章节"

git push
```

6. 1～2 分钟后，网站左侧导航就会出现新章节。

## 五、操作三：删除一个章节

### 示例目标

删除“03-进阶项目实操”这一整个章节。

### 步骤

1. 在 `docs/` 文件夹里直接删除对应文件夹：

```
docs/03-进阶项目实操/
```

2. 打开 `mkdocs.yml`，删除 `nav:` 里对应的两行：

```yaml
  - 进阶项目实操:                                    # 删除这行
    - 实战项目介绍: 03-进阶项目实操/项目介绍.md       # 删除这行
```

3. 保存，在 PowerShell 执行：

```powershell
git add .

git commit -m "删除进阶项目章节"

git push
```

**注意：文件夹和 `mkdocs.yml` 两处都要删干净，否则页面会显示“找不到文件”。**

## 六、操作四：上传并播放一个新的 PPT

### 原理说明

上传 PPT 后，网站会做两件事：

1. 自动把 PPT 每一页导出成高清图片，做成可翻页的静态浏览
2. 通过微软 Office 在线查看器提供“点击播放动画”的互动演示

这两件事都是自动的，你只需要做下面几步。

### 步骤

1. 把 `.pptx` 文件复制进 `docs/` 的某个文件夹，例如：

```
docs/资源下载/第一讲课件.pptx
```

2. 打开 `mkdocs.yml`，在 `nav:` 的资源下载部分加入一行：

```yaml
  - 资源下载:
    - 课件在线预览: 资源下载/课程课件.md
    - 第一讲课件: 资源下载/第一讲课件.md       # 新增这行
```

**注意：这里写的是 `.md` 后缀，不是 `.pptx`。** 这个 `.md` 是系统自动生成的，你不需要自己创建。

3. 保存 `mkdocs.yml`，执行：

```powershell
git add .

git commit -m "上传第一讲课件"

git push
```

4. 1～2 分钟后，左侧导航点击“第一讲课件”，就能看到：

- 上方“互动演示”：点击鼠标播放动画，跟 PowerPoint 放映一样
- 下方静态图片：逐页浏览、自动播放、缩略图跳转

### 更新已有 PPT

如果老师修改了同一个 PPT：

1. 用新文件覆盖 `docs/` 里原来的 `.pptx`（文件名保持不变）
2. 执行：

```powershell
git add .

git commit -m "更新第一讲课件"

git push
```

如果替换后网页内容没有立即变化，按 `Ctrl + F5` 强制刷新浏览器。

## 七、常用 Markdown 语法速查

| 想要的效果 | 写法 |
|---|---|
| 一级标题 | `# 标题文字` |
| 二级标题 | `## 标题文字` |
| 加粗 | `**加粗文字**` |
| 斜体 | `*斜体文字*` |
| 无序列表 | `- 列表项` |
| 有序列表 | `1. 列表项` |
| 链接 | `[显示文字](网址)` |
| 图片 | `![说明文字](图片路径)` |
| 代码块 | 上下各三个反引号，中间写代码 |
| 表格 | 用 `|` 分隔列，第二行用 `---` 分隔 |

## 七点五、嵌入清华云盘视频

课程视频不建议放进 Git 仓库（文件太大），应该上传到清华云盘，然后在网页里直接播放。

### 步骤

1. 在清华云盘上传视频文件
2. 对视频文件生成"下载链接"（共享外链），得到类似这样的地址：

```
https://cloud.tsinghua.edu.cn/f/e9a40e45abc34f1386f0/
```

3. 在 Markdown 文件里写：

```text
{{ cloud_video("https://cloud.tsinghua.edu.cn/f/e9a40e45abc34f1386f0/", "第一讲视频") }}
```

第一个参数是外链地址，第二个参数是可选标题。

4. 保存并执行一键更新。构建时系统会自动：
   - 访问云盘外链页面
   - 提取真正的视频直链
   - 生成一个带播放、暂停、进度条、全屏的视频播放器

### 注意

- 视频存放在云盘，网站仓库里只有一行占位符，体积不会膨胀
- 如果云盘外链失效或网络不通，页面会自动退化成普通链接，不影响其他内容

## 八、常见问题

### 问题 1：`git push` 报认证失败

说明 Git 没有记住清华 Git 的登录信息。重新执行 `git push`，在弹出的登录框里：

- 用户名：你的清华 Git 用户名
- 密码：清华 Git 的 Personal Access Token（不是登录密码）

Token 在清华 Git 网站的“设置 → Access Tokens”里生成，权限勾选 `write_repository`。

### 问题 2：改了内容，网页没变化

- 先按 `Ctrl + F5` 强制刷新
- 再用无痕窗口打开确认
- 检查是否真的执行了 `git push` 并且没有报错

### 问题 3：新 PPT 能下载但网页预览不出来

- 确认 `mkdocs.yml` 里写的是 `.md` 后缀而不是 `.pptx`
- 确认 `mkdocs.yml` 的缩进正确
- PPT 里的动画、复杂 SmartArt 可能无法 100% 还原，这属于格式限制

### 问题 4：想修改 PPT 的互动演示

互动演示由微软在线服务提供，PPTX 必须放在公网可访问的位置（本项目已托管在 GitHub Pages）。如果校内网络无法访问微软服务，可先使用页面下方的静态图片浏览。

## 九、小结

记住一条主线：

```
改 docs/ 里的文件（或 mkdocs.yml）→ git add . → git commit → git push → 等 1 分钟刷新
```

所有操作都归结为这条主线。只要文件放对位置、导航配置写对、最后 push 成功，网站就会自动更新。

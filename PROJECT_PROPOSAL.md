# RepoWatch 项目规划文档

> 撰写日期：2026-07-21
> 本文档用于与指导老师核对项目定位、已有条件及后续开发计划。

---

## 一、项目名称

**RepoWatch —— 代码仓库实时追踪与可视化平台**

（备选名称：GitEye / CodeTrace，最终名称待与老师确认）

---

## 二、项目背景

在课程项目开发过程中，学生通常面临以下问题：

1. 多人协作时，代码变更频繁但缺乏可追溯的记录，难以回顾"谁在什么时候改了什么"。
2. 各类项目文件（Python 代码、Markdown 文档、PDF 报告、PPTX 演示、DOCX 文档）散落在文件夹中，缺少统一的浏览和查看入口。
3. Git 虽然是版本控制的标准工具，但对不熟悉命令行的同学门槛较高，提交历史、文件对比等操作不够直观。

基于以上痛点，本项目旨在开发一个**基于 Web 的文件仓库管理与可视化系统**，以图形化界面的方式降低 Git 使用门槛，同时为课程项目的展示、协作与成果归档提供一个集中平台。

---

## 三、实验目的

1. 构建一个 Web 应用，自动监听本地项目文件夹的代码变更，并以 Git 方式自动记录版本历史。
2. 提供友好的网页前端界面，支持代码文件（Python、JavaScript、Markdown 等 30+ 语言）的语法高亮浏览，以及 PDF、PPTX、DOCX 等常见文档格式的内嵌预览。
3. 部署至老师提供的服务器，实现局域网内多人同时访问，作为课程项目的公共展示与协作入口。
4. 在此过程中学习并实践以下技术概念：
   - 前后端分离的基本架构（FastAPI 后端 + 模板渲染前端）
   - 文件系统监听与自动化的基本机制（watchdog + GitPython）
   - 远程服务器部署的基本流程（RustDesk 远程管理、端口开放、局域网访问）
   - 容器化交付的基本概念（Docker）

---

## 四、现有条件

### 4.1 硬件与网络环境

- 老师已提供一台开发服务器，已安装 Windows 操作系统。
- 已通过 **RustDesk** 实现对该服务器的远程桌面控制。
- 服务器位于校园局域网内，同一网络下的设备可通过 IP 地址相互访问。

### 4.2 已完成的软件功能（当前版本）

| 功能模块 | 状态 | 说明 |
|---|---|---|
| 自动仓库识别 | 已完成 | 将文件夹放入 `watched_repos/` 后系统自动识别并 Git init |
| 文件变更监听 | 已完成 | 基于 watchdog 库，监听文件增删改，防抖 2 秒后自动 `git add` + `git commit` |
| 文件树浏览 | 已完成 | 左侧可折叠侧边栏，按目录层级展示所有文件 |
| 代码查看器 | 已完成 | highlight.js 语法高亮（30+ 语言），行号显示，一键复制代码 |
| PDF 预览 | 已完成 | iframe 内嵌显示，浏览器原生渲染，不触发下载 |
| PPTX 幻灯片查看 | 已完成 | 提取文本、表格、图片，以幻灯片形式逐页渲染 |
| DOCX 文档预览 | 已完成 | 提取标题层级、段落、无序/有序列表、表格，按原文档顺序展示 |
| SSE 实时通知 | 已完成 | 文件自动提交后，页面顶栏实时显示同步状态 |
| REST API | 已完成 | `/api/repos` 查看仓库列表，`/api/repos/{name}/commits` 查看提交历史 |

### 4.3 技术栈

| 层级 | 技术 | 用途 |
|---|---|---|
| 后端框架 | FastAPI (Python) | 路由、请求处理、模板渲染 |
| Web 服务器 | Uvicorn | ASGI 服务器，监听 8765 端口 |
| Git 操作 | GitPython | 仓库初始化、文件暂存、自动提交 |
| 文件监听 | watchdog | 操作系统级文件变更事件监听 |
| 文档解析 | python-pptx / python-docx / Pillow | PPTX、DOCX 内容提取 |
| 模板引擎 | Jinja2 | 服务端 HTML 渲染 |
| 前端样式 | Tailwind CSS (CDN) | 整体 UI 风格 |
| 代码高亮 | highlight.js (CDN) | 代码着色 |
| 实时推送 | Server-Sent Events (SSE) | 提交通知实时推送至浏览器 |

### 4.4 当前运行方式

```bash
python -m uvicorn main:app --host 127.0.0.1 --port 8765
```

浏览器访问 `http://127.0.0.1:8765` 即可使用。

---

## 五、待开发功能与阶段性规划

### 第一期：局域网多人访问（当前优先）

| 任务 | 说明 |
|---|---|
| 修改监听地址 | 将 `config.json` 中 `host` 从 `127.0.0.1` 改为 `0.0.0.0`，允许外部设备访问 |
| 服务器部署 | 在老师提供的服务器上启动 RepoWatch，确认进程持续运行 |
| 防火墙与端口 | 确认服务器防火墙开放 8765 端口，局域网内其他设备可正常访问 |
| 验证 | 同学和老师通过浏览器访问 `http://服务器IP:8765`，多人同时查看仓库内容 |

### 第二期：项目介绍与多页面结构

| 任务 | 说明 |
|---|---|
| 关于页面 | 新建 `templates/about.html`，展示项目背景、技术栈、团队成员等信息 |
| 导航栏扩展 | 在顶部导航栏添加"首页 / 关于 / 成果展示"等链接 |
| 成果展示页 | 如老师有论文集、专利、项目成果等，可新增展示页面，支持图文混排 |
| 界面美化 | 根据课程主题定制配色与 Logo，增强辨识度 |

### 第三期：协作优化（视需求决定）

| 任务 | 说明 |
|---|---|
| 多仓库隔离 | 为不同的课程子项目建立独立的 watched repo，互不干扰 |
| 提交历史可视化 | 在仓库详情页增加提交时间线，展示历史变更记录 |
| 简易用户标注 | 允许在提交时标注操作者姓名（当前默认读取系统用户名），便于多人协作追溯 |

### 第四期（可选）：容器化交付

| 任务 | 说明 |
|---|---|
| 编写 Dockerfile | 定义项目的运行环境（Python 版本、依赖安装、启动命令） |
| 编写 docker-compose.yml | 一键启动服务，挂载 watched_repos 目录 |
| 交付验证 | 在其他机器上执行 `docker run` 即可运行，无需手动配置 Python 环境 |

---

## 六、关键概念说明

### 6.1 RustDesk 与多人访问的区别

- **RustDesk**（远程桌面）：开发者（我）用来操控服务器的工具。它传输的是桌面画面，同一时间只有一个人能看到和操作服务器的桌面。
- **浏览器直接访问**：用户（老师和同学）在自己的电脑上打开浏览器，输入 `http://服务器IP:8765` 即可使用 RepoWatch。这才是"多人同时使用"的正确方式。

结论：RustDesk 是**管理入口**（遥控器），浏览器访问是**使用入口**（大门）。两者不冲突，各司其职。

### 6.2 前端与后端的分工

- **后端（FastAPI + Python）**：负责读取硬盘上的文件、执行 Git 操作、解析 PDF/PPTX/DOCX 等二进制文件，将数据打包成 HTML 页面发送给浏览器。后端不关心按钮是什么颜色、页面怎样布局。
- **前端（HTML + Tailwind CSS + JavaScript）**：负责把后端传来的数据渲染成用户看到的界面——文件树、代码着色、复制按钮、PDF 内嵌等。前端不直接访问硬盘或执行 git 命令。

类比：后端是餐厅后厨（备菜、烹饪），前端是大堂（菜单、摆盘、上菜）。用户（食客）只跟大堂打交道，但整条链路缺一不可。

### 6.3 Docker 的意义

Docker 是一种"软件打包"方式。它把 Python 环境、所有依赖包、启动命令打包成一个镜像，任何安装了 Docker 的机器都可以用一条命令运行，无需手动配置环境。对于本项目而言，Docker 化的主要价值在于**降低老师的部署成本**：老师拿到项目后不需要安装 Python 和依赖，直接 `docker run` 即可使用。

### 6.4 SaaS 概念的延伸理解

SaaS（Software as a Service）是指软件部署在云端服务器上，用户通过浏览器登录使用，数据存储在云端。当前的 RepoWatch 是一个本地工具，但后续可以演进为 SaaS 形态：部署到云服务器、增加用户注册与登录功能、实现多用户数据隔离。这是一个长期方向，不在本学期的交付范围内。

---

## 七、项目文件结构

```
repo-watcher/
├── main.py              # FastAPI 入口，所有路由定义
├── git_manager.py       # Git 操作（init/add/commit）+ 文件内容提取
├── watcher.py           # watchdog 文件变更监听 + 防抖逻辑
├── config.json          # 配置文件（端口、防抖时长等）
├── requirements.txt     # Python 依赖列表
├── templates/           # 前端 HTML 模板（Jinja2）
│   ├── base.html        #   基础模板（导航栏 + SSE 通知）
│   ├── repos.html       #   仓库列表首页
│   ├── repo.html        #   仓库详情页（文件树）
│   ├── file.html        #   代码查看器
│   ├── pdf.html         #   PDF 阅读器
│   ├── ppt.html         #   PPTX 幻灯片查看器
│   └── docx.html        #   DOCX 文档查看器
├── watched_repos/       # 被监听的仓库目录
│   └── demo-project/    #   示例项目（含各类文件用于测试）
└── static/              # 静态资源目录
```

---

## 八、待与老师确认的事项

1. 项目最终名称是否合适？有没有推荐的命名方向？
2. 服务器是否已开放 8765 端口？若未开放，是否需要协助配置防火墙规则？
3. 第二期的"项目介绍"和"成果展示"页面，老师是否有具体的内容要求（如展示哪些论文、成果、团队信息）？
4. 是否需要增加用户登录功能，以实现不同学生账号的权限隔离？还是当前的无认证共享模式已满足课程需求？
5. 对于 Docker 容器化交付，老师是否有硬性要求？若无，是否作为加分项处理？
6. 项目最终提交形式是源代码 + 部署说明，还是需要额外的实验报告 / 答辩材料？

---

*本文档将随项目进展持续更新。*

# 函数与模块

## 定义函数

```python
def greet(name: str) -> str:
    """返回问候语"""
    return f"你好，{name}！"

print(greet("张三"))  # 你好，张三！
```

### 参数类型

```python
# 位置参数
def add(a, b):
    return a + b

# 默认参数
def power(base, exp=2):
    return base ** exp

print(power(3))     # 9  (3^2)
print(power(3, 3))  # 27 (3^3)

# 关键字参数
def describe_person(name, age, city="北京"):
    print(f"{name}, {age}岁, 来自{city}")

describe_person(name="张三", age=20)
describe_person(age=22, name="李四", city="上海")
```

### 可变参数

```python
# *args: 任意数量的位置参数
def sum_all(*args):
    return sum(args)

print(sum_all(1, 2, 3, 4, 5))  # 15

# **kwargs: 任意数量的关键字参数
def print_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_info(name="张三", age=20, score=95)
```

## 模块

模块就是 `.py` 文件。你可以把函数放在一个文件里，在另一个文件里导入使用。

### 创建模块

```python
# 文件: math_utils.py
def add(a, b):
    return a + b

def multiply(a, b):
    return a * b

PI = 3.14159
```

### 导入模块

```python
# 方式一：导入整个模块
import math_utils
print(math_utils.add(10, 5))
print(math_utils.PI)

# 方式二：导入特定函数
from math_utils import add, multiply
print(add(10, 5))

# 方式三：导入全部（不推荐）
from math_utils import *
```

## 匿名函数 lambda

```python
# 普通函数
def square(x):
    return x ** 2

# lambda 等价写法
square = lambda x: x ** 2

# 常见用法：配合 sorted、map、filter
students = [
    {"name": "张三", "score": 85},
    {"name": "李四", "score": 92},
    {"name": "王五", "score": 78},
]

# 按分数排序
ranked = sorted(students, key=lambda s: s["score"], reverse=True)
print(ranked)
```

## 作业模板

下载作业模板，补全代码并提交：

```python
--8<-- "docs/02-Python基础语法/作业模板.py"
```

!!! example "练习"
    1. 打开 [作业模板](作业模板.py)，补全 `search_by_name` 和 `update_score` 方法
    2. 实现 `get_top_students(n)` 方法，返回成绩前 N 名的学生
    3. 在 `main()` 中编写测试用例验证你的实现

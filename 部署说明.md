# 课程网站部署说明

> 本文档面向运维同学：如何在清华 Git + 校园服务器上部署课程网站，实现 push 自动更新。

## 一、整体架构

```
清华 Git（代码 + 权限）
    │ git push
    ▼
GitLab Runner（服务器上的自动构建进程）
    │ mkdocs build
    ▼
静态文件 → Nginx（服务器上的网页服务）
    │
    ▼
学生浏览器访问 http://服务器IP:端口/
```

## 二、最简部署：Docker 一条命令

如果服务器已装 Docker，不需要 Runner，直接构建并启动：

```bash
docker compose up -d --build
```

然后访问 `http://服务器IP:8080/`。

这是"手动构建"模式：每次代码更新后重新执行上面的命令。

## 三、自动部署：GitLab Runner

要实现"push 自动更新"，需要在服务器上注册 Runner。

### 1. 安装 Docker（如未安装）

Ubuntu 示例：

```bash
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-plugin
sudo systemctl enable --now docker
```

### 2. 注册 Runner

在清华 Git 项目 → Settings → CI/CD → Runners → New project runner，拿到注册命令（形如 `gitlab-runner register --url ... --token ...`）。

在服务器上执行：

```bash
docker run --rm -it \
  -v /srv/gitlab-runner/config:/etc/gitlab-runner \
  gitlab/gitlab-runner:latest register \
  --non-interactive \
  --url https://git.tsinghua.edu.cn \
  --token <注册令牌> \
  --executor shell \
  --tag-list course-server
```

### 3. 启动 Runner

```bash
docker run -d --name gitlab-runner --restart unless-stopped \
  -v /srv/gitlab-runner/config:/etc/gitlab-runner \
  -v /var/run/docker.sock:/var/run/docker.sock \
  gitlab/gitlab-runner:latest
```

之后每次 `git push`，Runner 会自动执行 `.gitlab-ci.yml`，构建并部署。

## 四、Nginx 多课程托管

当有多门课程时，用 Nginx 统一管理，每门课一个子路径：

```nginx
server {
    listen 80;
    server_name 课程网站域名或IP;

    location /python-course/ {
        alias /srv/course-sites/python-course/;
        index index.html;
    }

    location /data-structure/ {
        alias /srv/course-sites/data-structure/;
        index index.html;
    }
}
```

每门课程的 `.gitlab-ci.yml` 里把 `DEPLOY_PATH` 改为对应目录：

```yaml
variables:
  DEPLOY_PATH: "/srv/course-sites/python-course"
```

## 五、环境要求清单

| 项目 | 要求 |
|---|---|
| 操作系统 | Ubuntu 20.04+（推荐）或 Windows Server |
| Docker | 20.10+ |
| Python | 3.10+（Docker 模式不需要） |
| 网络 | 能访问清华 Git 和清华云盘 |
| 端口 | 80/443（Nginx）或自定义端口 |

## 六、故障排查

### 构建失败

在 Runner 日志里查看报错，常见原因：

- 清华云盘外链失效 → 视频页会降级成链接，不影响构建
- PPTX 无法转换 → 检查服务器是否装了 LibreOffice 和中文字体
- 网络不通 → 确认服务器能访问清华 Git

### 网站 404

- 检查 Nginx 配置的 `alias` 路径是否正确
- 检查 `.gitlab-ci.yml` 的 `DEPLOY_PATH` 是否和 Nginx 一致

### 端口被占用

修改 `docker-compose.yml` 的端口映射，例如 `8080:80` 改成 `9090:80`。

# 课程网站部署说明（已跑通版本）

> 本文件记录 2026-09-04 在校园服务器上实际验证通过的部署架构。

## 一、整体架构

```
老师修改内容 → git push 到清华 Git
                    │
                    ▼
           GitLab Runner（服务器 Docker 容器）
                    │ 自动触发
                    ▼
           docker compose up -d --build
                    │ 重建镜像并重启
                    ▼
           nginx 容器（端口 8080）
                    │
                    ▼
           学生访问 http://服务器IP:8080/
```

## 二、服务器信息

| 项目 | 值 |
|---|---|
| 系统 | Ubuntu 22.04 LTS |
| 用户 | tianyi |
| 内网 IP | 10.10.10.104 |
| 网站端口 | 8080 |
| 代码位置 | ~/repowatch |
| Runner 配置 | /srv/gitlab-runner/config |

## 三、关键配置清单

### 1. Docker 镜像源（已配置）

文件：`/etc/docker/daemon.json`

```json
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://hub-mirror.c.163.com",
    "https://docker.1panel.live"
  ]
}
```

### 2. Dockerfile（多阶段）

- 阶段一：`python:3.12-slim` + LibreOffice + 中文字体 + git，执行 `mkdocs build`
- 阶段二：`nginx:1.27-alpine` 托管构建产物
- apt/pip 均使用清华镜像加速

### 3. GitLab Runner（Docker 执行器）

- 镜像：`docker:27`
- 挂载：`/var/run/docker.sock`（让任务能控制宿主机 Docker）
- 标签：`course-server`
- Runner 以 Docker 容器方式运行，配置保存在 `/srv/gitlab-runner/config`

### 4. CI 配置（.gitlab-ci.yml）

```yaml
deploy:
  stage: deploy
  tags:
    - course-server
  image: docker:27
  script:
    - docker compose up -d --build
  only:
    - main
```

原理：Runner 收到 push 后，启动一个 docker:27 容器，在该容器里执行
`docker compose up -d --build`。由于 docker.sock 已挂载，这个命令实际
控制的是宿主机 Docker，会重建并重启课程网站容器。

## 四、日常维护命令

### 查看网站容器状态

```bash
docker ps
```

### 查看 Runner 状态

```bash
docker ps | grep gitlab-runner
```

### 查看 Runner 日志（排查流水线问题）

```bash
docker logs gitlab-runner
```

### 手动重建网站（不通过 CI）

```bash
cd ~/repowatch
git pull
docker compose up -d --build
```

### 重启 Runner

```bash
docker restart gitlab-runner
```

## 五、已知问题与说明

### 1. 代理环境变量

服务器全局设置了校园代理（HTTP_PROXY=10.10.10.5:10820），影响 curl 等命令。
访问本机服务时加 `--noproxy '*'`，例如：

```bash
curl --noproxy '*' -I http://127.0.0.1:8080/
```

不要删除代理配置，否则服务器可能无法访问外网。

### 2. 网络访问范围

- 服务器本机：`http://127.0.0.1:8080/` 可访问
- 校内 10.x 网段设备：预期可访问 `http://10.10.10.104:8080/`（待校园内网设备验证）
- 公网 WiFi（183.x）：需要校园 VPN 或学校端口映射
- 公网兜底：GitHub Pages（https://crooks160449-create.github.io/ripowatch/）

### 3. 清华云盘视频

视频文件放在清华云盘，网站构建时通过 hooks/cloud_media.py 自动解析外链为直链。
若云盘外链失效，视频区域自动退化为普通链接，不影响构建。

## 六、多课程扩展思路

当前架构是"一个项目一个容器"。多门课程时：

1. 每门课在清华 Git 建独立仓库
2. 每门课复制本项目，改 `mkdocs.yml` 的 `site_name`
3. 修改 `docker-compose.yml` 的端口映射（如 8081:80）
4. 每门课注册独立 Runner（标签不同，如 `course-server-2`）
5. 用宿主机 Nginx 统一反代不同端口

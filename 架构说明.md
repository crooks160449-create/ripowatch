# RepoWatch 项目架构说明

> 本文档用于完整掌握项目：每个功能怎么实现、代码怎么配合、数据怎么流动。
> 建议像给新人讲解项目一样阅读。

## 一、项目是什么

RepoWatch 是一个**课程资源网站系统**：老师编辑 Markdown/PPT/图片，系统自动构建成网站并自动更新。

## 二、总体架构

```
┌─────────────┐    编辑文件     ┌──────────────┐
│  老师/助教   │ ──────────────> │  共享文件夹    │
│  Windows    │   (Samba)      │  course-content│
└─────────────┘                └───────┬──────┘
                                       │ watcher 监听（2秒防抖）
                                       ▼
                              自动 git add/commit/push
                                       │
                                       ▼
                             清华 Git repowatch-content
                                       │
                                       ▼
                             GitLab Runner 自动触发
                                       │
                                       ▼
                              docker compose 重建
                                       │
                                       ▼
                              nginx 提供网页
                                       │
                                       ▼
                              http://10.10.10.41:8080
```

## 三、双仓库设计

| 仓库 | 角色 | 内容 |
|---|---|---|
| `repowatch` | 框架仓库（开发者） | mkdocs.yml、hooks、Dockerfile、部署配置 |
| `repowatch-content` | 内容仓库（老师） | docs 内容、watcher、自动部署 CI |

分离的原因：老师不需要碰框架代码，开发者不需要碰课程内容。

## 四、数据流：从编辑到网站的完整旅程

1. 老师通过 Samba 共享，在 Windows 上编辑文件（实际写在服务器 `/home/libs/course-content/`）
2. watcher（systemd 服务）用 watchdog 监听到变化，停止 2 秒后触发同步
3. watcher 执行 `git add + commit + push`，推送到清华 Git 的 `repowatch-content`
4. GitLab 收到 push，把任务分发给 Runner（标签 `course-server` 匹配）
5. Runner 在 docker:27 容器里执行 `.gitlab-ci.yml`：
   - `docker rm -f repowatch-course-site`（清旧容器）
   - `docker compose up -d --build`（重建）
6. Dockerfile 多阶段构建：Python 装依赖 → LibreOffice 转 PPT → mkdocs build → nginx 托管
7. nginx 容器监听 8080 端口，提供网页

## 五、功能 → 代码端到端映射

### 功能1：课程内容展示

| 环节 | 代码/文件 | 作用 |
|---|---|---|
| 内容 | `docs/*.md` | Markdown 课程页面 |
| 配置 | `mkdocs.yml` | 主题、导航、插件 |
| 构建 | `mkdocs build` | 把 md 转成 HTML |
| 主题 | Material 主题 | 导航、搜索、深色模式 |

### 功能2：PPT 在线播放

| 环节 | 代码 | 作用 |
|---|---|---|
| 输入 | `docs/资源下载/*.pptx` | 课件文件 |
| 转换 | `hooks/pptx_converter.py` | 构建时把 PPT 转成逐页图片 |
| 哈希校验 | `.source-hash` 文件 | 判断 PPT 是否更新过 |
| 渲染 | 生成的 `*.md` | 幻灯片播放器（翻页/放大/自动播放） |
| 互动 | Office 在线查看器 iframe | 点击播放动画 |

### 功能3：清华云盘视频

| 环节 | 代码 | 作用 |
|---|---|---|
| 占位符 | `{{ cloud_video("外链") }}` | 写在 md 里的标记 |
| 解析 | `hooks/cloud_media.py` | 构建时抓取外链页面，提取 seafhttp 直链 |
| 降级 | 解析失败时 | 退回普通链接，不中断构建 |

### 功能4：图片画廊

| 环节 | 代码 | 作用 |
|---|---|---|
| 占位符 | `{{ gallery("assets/gallery") }}` | 指定图片目录 |
| 生成 | `hooks/gallery.py` | 扫描目录，生成网格 + 灯箱 |
| 交互 | 内嵌 JS | 点击放大、左右切换、键盘导航 |

### 功能5：自动同步（老师无感）

| 环节 | 代码 | 作用 |
|---|---|---|
| 共享 | Samba（smb.conf） | 让 Windows 访问服务器文件夹 |
| 监听 | `内容同步工具.py`（watchdog） | 检测文件变化 |
| 防抖 | 2 秒 Timer | 避免频繁提交 |
| 提交 | git add/commit/push | 自动推送 |
| 自启 | systemd `course-sync.service` | 开机后台运行 |

### 功能6：自动部署

| 环节 | 代码 | 作用 |
|---|---|---|
| 触发 | `.gitlab-ci.yml` | push 后执行构建 |
| 执行 | GitLab Runner（Docker executor） | 运行构建任务 |
| 构建 | Dockerfile | 多阶段构建镜像 |
| 托管 | nginx 容器 | 8080 端口服务 |

## 六、关键文件清单

### 框架仓库（repowatch）

| 文件 | 作用 |
|---|---|
| `mkdocs.yml` | 网站配置 |
| `hooks/pptx_converter.py` | PPT 转图片 |
| `hooks/cloud_media.py` | 云盘视频解析 |
| `hooks/gallery.py` | 图片画廊 |
| `Dockerfile` | 镜像构建 |
| `docker-compose.yml` | 服务编排 |
| `requirements.txt` | Python 依赖 |

### 内容仓库（repowatch-content）

| 文件 | 作用 |
|---|---|
| `docs/` | 课程内容 |
| `内容同步工具.py` | watcher |
| `.gitlab-ci.yml` | 自动部署 |

### 服务器

| 位置 | 作用 |
|---|---|
| `/home/libs/course-content/` | 共享文件夹（内容仓库） |
| `/etc/samba/smb.conf` | Samba 共享配置 |
| `/etc/systemd/system/course-sync.service` | watcher 服务 |
| `/home/libs/gitlab-runner/config/config.toml` | Runner 配置 |

## 七、给新人讲解项目的脉络

1. **先讲问题**：课程文件散乱、更新麻烦
2. **再讲方案**：MkDocs 把 Markdown 变网站
3. **讲自动更新**：watchdog 监听 → git push → Runner 构建
4. **讲仓库分离**：框架/内容分开，老师只需编辑内容
5. **讲部署**：Docker 打包，服务器上一条命令运行

## 八、技术栈总结

| 层 | 技术 |
|---|---|
| 内容 | Markdown |
| 建站 | MkDocs + Material |
| 转换 | python-pptx、LibreOffice |
| 监听 | watchdog |
| 版本 | Git + GitLab（清华 Git） |
| CI | GitLab Runner |
| 部署 | Docker + nginx |
| 共享 | Samba |
| 服务管理 | systemd |
